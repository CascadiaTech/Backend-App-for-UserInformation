  } else {

    async function Fetchaccnts() {
      try {
        const uri = 'mongodb+srv://Flaskapp:iamdiablo812@clustertest.rxvyc0x.mongodb.net/?retryWrites=true&w=majority'
        const client = new MongoClient(uri)
        await client.connect()
        const database = client.db('Orders')
        const movies = database.collection('orders')
        const cursor = await movies.find()
        const allValues = await cursor.toArray()
        console.log(allValues)
        //const final = await cursor.forEach((doc) => console.log(doc))
        return final.json()
      } catch (error) {
        console.log(error)
      } finally {
        const client = new MongoClient(uri)
        await client.close()
      }
    }
    Fetchaccnts()
    response.writeHead(200, { 'Content-Type': 'application/json' })
    response.end(html)
  }